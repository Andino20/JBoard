package plus.sprak.app.messages;

/**
 * Maybe you don't need a game state message, but if you end up needing one you should probably
 * give each piece and ID and transmit and array of (ID, position, color) or something like that
 * for each piece. Again this is just an example.
 */
public class GameStateMessage extends GameMessage {

}
